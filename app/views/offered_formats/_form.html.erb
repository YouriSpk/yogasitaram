<div class="container mt-5">
  <%= simple_form_for(@offered_format, html: { class: 'form-horizontal' }) do |f| %>
    <% if @offered_format.errors.any? %>
      <div class="alert alert-danger">
        <h4>Veuillez corriger les erreurs suivantes :</h4>
        <ul>
          <% @offered_format.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label :title, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.input :title, label: false, class: 'form-control' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :description, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.input :description, label: false, as: :text, input_html: { class: 'form-control' } %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.button :submit, class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>
